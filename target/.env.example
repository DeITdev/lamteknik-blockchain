# Target stack — copy to target/.env for local overrides (optional)
# MySQL host port when using target/docker-compose.yml
DB_HOST=localhost
DB_PORT=3307
DB_USERNAME=lamtek
DB_PASSWORD=lamtek123
DB_DATABASE=lamtek_db

# CDC user (created by backend/database/init/02-cdc-user.sql)
CDC_USER=cdc_user
CDC_PASSWORD=cdc_pass
