services:
  gateway:
    build: .
    container_name: lamteknik-gateway
    network_mode: host
    env_file:
      - .env
    volumes:
      - ./build/chains:/app/build/chains
      - ../blockchain/hyperledger-fabric/docker/explorer/organizations:/fabric/organizations:ro
    restart: unless-stopped
