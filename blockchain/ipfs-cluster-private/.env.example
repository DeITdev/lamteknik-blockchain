# Shared secret for IPFS Cluster CRDT peers (32-byte hex).
# Generate: openssl rand -hex 32
CLUSTER_SECRET=

# Private Kubo swarm key — generate with scripts/generate-swarm-key.sh
# File: swarm.key (not stored in git)
